# Same Postgres instance as pooly-core (Railway project), CRM owns only the crm_prospects table
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/pooly

# Must match pooly-core's JWT_SECRET_KEY exactly — tokens from pooly-core's /login are accepted here
JWT_SECRET_KEY=super-secret

PORT=8081
